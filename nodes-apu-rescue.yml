build:
  platform: x86
  platform_type: '64'
  openwrt_version: 23.05.5
  directory: files-apu-rescue

apu-rescue:
  profile: 'generic'
  hostname: 'apu-rescue'
  packages: 'wpad-mesh-mbedtls kmod-mt7915-firmware kmod-mt7915e luci-ssl iperf3 git openssh-client-utils openssh-client openssh-server openssh-sftp-server'
