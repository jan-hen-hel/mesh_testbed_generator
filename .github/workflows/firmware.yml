name: Building Firmware Images
run-name: Building Firmeware Images
on: [push]
jobs:
  Build:
    runs-on: ubuntu-24.04
    env:
        RCLONE_CONFIG_RSHARE_VENDOR: nextcloud
        RCLONE_CONFIG_RSHARE_TYPE: webdav
        RCLONE_CONFIG_RSHARE_PASS: ${{ secrets.RCLONE_CONFIG_RSHARE_PASS }}
        RCLONE_CONFIG_RSHARE_URL: ${{ secrets.RCLONE_CONFIG_RSHARE_URL }}
        RCLONE_CONFIG_RSHARE_USER: ${{ secrets.RCLONE_CONFIG_RSHARE_USER }}

    steps:
      - name: "Checkout Sources"
        uses: actions/checkout@v4
      - name: "Setup Ruby"
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1' 
      - name: Restore Download Cache
        id: cache-restore
        uses: actions/cache/restore@v4
        with:
          path: |
            openwrt-imagebuilder-23.05.5-x86-64.Linux-x86_64
            openwrt-imagebuilder-23.05.5-x86-64.Linux-x86_64.tar.xz
          key: download-cache
      - name: Install Build depdencies
        run: sudo apt-get install -y rake build-essential make gawk rclone 
      - run: rake
        name: Building Firmware
      - uses: actions/upload-artifact@v4
        name: Uploading Firmware Artifacts
        with:
            name: firmware
            path: bin/*-squashfs-combined.img.gz
      - name: Save Download Cache
        id: cache-save
        uses: actions/cache/save@v4
        with:
          path: |
            openwrt-imagebuilder-23.05.5-x86-64.Linux-x86_64
            openwrt-imagebuilder-23.05.5-x86-64.Linux-x86_64.tar.xz
          key: ${{ steps.cache-restore.outputs.cache-primary-key }}
      - name: Deploy to Nextcloud
        run: rclone copy bin rshare: